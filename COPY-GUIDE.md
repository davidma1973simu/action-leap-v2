# Action Leap · 文案 & 视觉语言规范（说人话，有温度）

> 适用：V2 桌面 HR 端（index.html）、手机学员端（student.html）
> 目标：抓得住 **Z 世代学员**（活泼、不爹味、有即时正反馈），也取悦得了 **甲方 / HR / 高层**（清晰、专业、汇报就绪）。
> 配套：`icons.js`（原创线性图标，零 emoji）、`design-system.css`（瑞士极简 × 微质感）

---

## 一、四条铁律

1. **零 emoji 进界面** —— 所有图标走 `icons.js`（`AL.icon('name')`），不用 🌱✅🎉🔥 等字符。原因：emoji 风格不统一、在部分系统渲染为彩色小人，拉低专业感与"设计感"。
2. **零图形黑话暴露给最终用户** —— 学员/甲方看不到 `JSON`、`SKU`、`项目包`、`配置文件`、`导入导出 Code`。底层是导入导出，对使用者说"提交码 / 进展码 / 分享链接"。
3. **说人话，先讲"这对我有什么用"** —— 不堆功能名词，先给价值，再给动作。
4. **少即是多** —— 一行能说清的不写两句；按钮文案 ≤6 字优先。

---

## 二、两群人，两种语气

| 维度 | 学员端（Z 世代） | HR / 甲方端 |
|------|----------------|------------|
| 语气 | 同伴感、轻、有梗不油腻 | 清晰、克制、可信 |
| 人称 | "你"（直接对话） | "学员""课程""成果" |
| 反馈 | 即时、有温度、带小鼓励 | 结构化、可汇报 |
| 动效 | 有生命感（弹跳/光晕/彩屑） | 克制（入场淡入/微浮） |
| 避免 | 说教、考核感 | 情绪化、含糊 |

---

## 三、学员端 · 关键文案（已落地）

| 位置 | 文案 | 思路 |
|------|------|------|
| 顶栏问候 | 「下午好」+ 姓名 | 用时段问候，像朋友 |
| 身份卡 | 「你正在成为谁？」+ 微光角标 | 把"身份认同"变成自我叙事，非考核 |
| 今日标题 | 「今天，迈出一小步」 | 降低行动门槛，强调"小" |
| 打卡状态 | 试了一步 / 在坚持 / 先放一放 | 三态去惩罚性，"先放一放"不羞辱 |
| 状态副文案 | 刚起步，先动起来 / 节奏稳了，保持住 / 今天没做到，没关系 | 每种状态都接住情绪 |
| 打卡保存 | 「记下来」 | 不是"提交""保存"，像随手记 |
| 庆祝层 | 「漂亮，第一步最难」/「稳了，节奏感来了」/「没关系，改变本就有起伏」 | 按状态给差异化正反馈 |
| 进展页 | 「我的成长轨迹」「稳住的习惯」「连续天数」 | 第一人称、成长感 |
| 提交 | 「发给培训负责人」「把进展交给负责人」「你的每一步，都值得被看见」 | 赋予意义，不是"上传数据" |
| 空状态 | 「内容还在准备中」「还没有记录哦，去『今日』点亮第一个习惯」+ 原创插画 | 共情 + 下一步 + 插画 |

## 四、HR 端 · 关键文案（已落地）

| 位置 | 文案 | 思路 |
|------|------|------|
| 侧栏品牌 | Action Leap / 培训成效 · 行为迁移 | 一句话讲清定位 |
| 汇报四块 eyebrow | 框架 Framework / 流程 Flow / 亮点 Highlights / 逻辑 Logic | 先框架后流程主线 |
| 复盘空态 | 「还没有学员的复盘数据。先到『学员端』添加学员，等他们用手机微打卡后，这里会自动出现每个人的成长轨迹。」+ 插画 | 说清因果，给路径 |
| 证据链 | 「培训价值证据链校验」「证据链总体状态：基本贯通 / 部分可汇报 / 需继续收集」 | 回答"钱花得值不值" |
| 按钮 | 复制文字版 / 导出汇报件 / AI 建议（基于打卡流） | 动词开头，说人话 |
| AI 标签 | 「AI 自动提炼」 | 不夸大，标清是自动合成 |

---

## 五、图标使用约定（icons.js）

- 调用：`AL.icon('today' | 'framework' | 'progress' | 'leaf' | 'send' | 'copy' | 'export' | 'ai' | 'spark' | 'target' | 'path' | 'check' | ...)`
- 内联对齐：给图标加 `cls:'al-ic'`（已定义 `vertical-align:-3px;margin-right:5px;color:品牌色`）。
- 静态位置（如顶栏/导航）：写 `<span data-ic="today"></span>` 并调用 `paintIcons()`，或直接写 `<svg class="ic-svg"><use href="#al-ic-today"/></svg>`。
- 风格：1.8px 描边、圆角端点、`currentColor` 着色 —— 随主题变深，不用多色块。

---

## 六、禁止清单（recheck 用）

- ❌ 任何 emoji 出现在可见界面
- ❌ 对学员/甲方出现 `JSON / SKU / 配置包 / 项目包 / 导入导出 Code`
- ❌ 把打卡说成"提交作业 / 考核 / 打分"
- ❌ 把 HR 汇报说成"数据看板 / 后台"（用"成果汇报 / 一页视图"）
- ❌ 长段落说明（>2 行且无重点）；能用卡片/标签就不用成段文字

---

## 七、改文案的五步自检

1. 这句话学员/甲方看得懂吗？有黑话吗？
2. 它先给了价值，还是先堆了名词？
3. 情绪是接住还是打压？（尤其失败/空态）
4. 能不能更短？
5. 配的图标/插画有没有提升"设计感"？
